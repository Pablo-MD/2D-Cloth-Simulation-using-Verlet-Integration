using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NodeCoord : MonoBehaviour
{
    //Class create to get the index of the 2D array in cloth generator when needed in the mouseController, not the most elegent way of doing it
    public Vector2Int coord;
}
